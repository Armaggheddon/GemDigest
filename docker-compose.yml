services:
  bot:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: gem_digest_bot
    volumes:
      - $PWD/website_blacklist.txt:/gem_digest_bot/src/constant/website_blacklist.txt
    env_file: 
      - ./api_keys.env